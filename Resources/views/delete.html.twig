{% extends kimai_context.modalRequest ? 'form.html.twig' : 'base.html.twig' %}

{% block main %}
    {{ include(kimai_context.modalRequest ? 'default/_form_delete_modal.html.twig' : 'default/_form_delete.html.twig', {
        'title': 'planner.delete_entry'|trans,
        'message': 'planner.delete_recurring_prompt'|trans,
        'form': form,
        'delete_item_name': activity.title,
        'used': true,
        'back': path('planner_week', {'date': (date is defined and date is not empty ? date : (activity.begin ? activity.begin|date('Y-m-d') : 'now'|date('Y-m-d')))})
    }) }}
{% endblock %}
